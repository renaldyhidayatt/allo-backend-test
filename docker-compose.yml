services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: split-bill-api
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - APP_GITHUB_USERNAME=renaldyhidayatt
    restart: unless-stopped
